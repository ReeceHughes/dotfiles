# Required for TMUX to load the bash scripts
# Order of bash initializtion
#  Login mode:
#      /etc/profile
#      ~/.bash_profile, ~/.bash_login, ~/.profile (only first one that exists)
#
#  Interactive non-login:
#      /etc/bash.bashrc (some Linux; not on Mac)
#      ~/.bashrc
# 

. ~/.bashrc

